#ifndef FUNCTIONS_H
#define FUNCTIONS_H

#include <iostream>
#include <string>
#include <thread>    // for sleep_for

void message(const std::string& text);  //to put text into a bubble
void delay(int milliseconds); //to delay the code
#endif // FUNCTIONS_H
